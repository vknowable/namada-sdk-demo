{
  "name": "@namada/shared",
  "version": "0.1.0",
  "main": "src/index.ts",
  "private": false,
  "dependencies": {
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  },
  "scripts": {
    "wasm:build": "wasm-pack build ./lib --release --target web --out-dir ../src/shared",
    "wasm:build:dev": "wasm-pack build ./lib --dev --target web --out-dir ../src/shared"
  }
}
